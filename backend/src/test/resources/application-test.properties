spring.application.name=hrm

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/hrm
spring.datasource.username=postgres
spring.datasource.password=sa
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# OAuth2 Google (?ã che ?? push code an toàn)
spring.security.oauth2.client.registration.google.client-id=xxx
spring.security.oauth2.client.registration.google.client-secret=xxx
spring.security.oauth2.client.registration.google.scope=email,profile

# JWT (?ã che ?? push code an toàn)
app.jwt.secret-key=xxx
app.jwt.access-token-expiry=900000
app.jwt.refresh-token-expiry=604800000

# Mail Configuration (?ã che m?t kh?u ?? push code an toàn)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dinhfuk5@gmail.com
spring.mail.password=xxx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend
app.frontend-url=http://localhost:5173